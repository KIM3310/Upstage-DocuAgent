# Optional
# If `UPSTAGE_API_KEY` is not set, the app runs in demo mode (stubbed outputs).
UPSTAGE_API_KEY=

# Force demo mode even when an API key is configured:
# DOCUAGENT_MODE=demo

# Optional server bind overrides
# DOCUAGENT_HOST=0.0.0.0
# DOCUAGENT_PORT=8000

# Optional Upstage overrides
# UPSTAGE_BASE_URL=https://api.upstage.ai/v1
# UPSTAGE_SOLAR_MODEL=solar-pro2
